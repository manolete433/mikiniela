<% include ../partials/header %>
    <div class="container">
        <div class="row">
            <h1 style="text-align: center;">
                <%= games[0].week.name %>
            </h1>
            <div class="container">
            <table>
                    <tr>
                        <th>Home Team</th>
                        <th>Away Team</th>
                        <th>Date</th>
                    </tr>
            <% games.forEach(function(game){ %>
                <tr>
                    <td><img src="<%= game.homeTeam.imageURL %>" class="show-team-logo" style="width: 3%; "><%= game.homeTeam.name %></td>
                    <td><img src="<%= game.awayTeam.imageURL %>" class="show-team-logo" style="width: 3%; "><%= game.awayTeam.name %></td>
                    <td><%= game.gameDate %></td>
                </tr>
            <% }) %>
            </table>
        </div>
                    <div style="text-align: center;">
                        <a class="btn btn-md btn-warning" href="/weeks/<%= games[0].week.id %>/edit">Edit</a>
                        <% if (games[0].week.isActive) { %>
                            <form class="update-form" action="/weeks/<%= games[0].week.id %>?_method=DELETE" method="POST">
                                <button class="btn btn-md btn-danger">Deactivate</button>
                            </form>
                            <% } else{ %>
                                <form class="update-form" action="/teams/<%= games[0].week.id %>/activate" method="POST">
                                    <button class="btn btn-md btn-success">Activate</button>
                                </form>
                                <% } %>
                                    <!-- Only if admin -->
                                    <div>
                                        <a href="/weeks">Go Back</a>
                                    </div>
                    </div>
        </div>
    </div>

    <% include ../partials/footer %>